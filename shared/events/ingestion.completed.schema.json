{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://eventforge.dev/schemas/ingestion.completed.schema.json",
  "title": "IngestionCompletedPayload",
  "description": "Payload for eventforge.ingestion.completed — mock sources stored for the job.",
  "type": "object",
  "required": ["source_ids", "source_count"],
  "properties": {
    "source_ids": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uuid"
      },
      "minItems": 1,
      "description": "Primary keys of sources rows written during ingestion."
    },
    "source_count": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of sources fetched and persisted."
    }
  },
  "additionalProperties": false
}
