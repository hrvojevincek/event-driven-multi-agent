{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://eventforge.dev/schemas/knowledge.mined.schema.json",
  "title": "KnowledgeMinedPayload",
  "description": "Payload for eventforge.knowledge.mined — mock entities extracted and stored for the job.",
  "type": "object",
  "required": ["entity_ids", "entity_count"],
  "properties": {
    "entity_ids": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uuid"
      },
      "minItems": 1,
      "description": "Primary keys of knowledge_entities rows written during knowledge mining."
    },
    "entity_count": {
      "type": "integer",
      "minimum": 1,
      "description": "Total number of entities mined for the job."
    }
  },
  "additionalProperties": false
}
