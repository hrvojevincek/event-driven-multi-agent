{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://eventforge.dev/schemas/embedding.completed.schema.json",
  "title": "EmbeddingCompletedPayload",
  "description": "Payload for eventforge.embedding.completed — mock document chunks embedded via pgvector.",
  "type": "object",
  "required": ["chunk_ids", "chunk_count"],
  "properties": {
    "chunk_ids": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uuid"
      },
      "minItems": 1,
      "description": "Primary keys of document_chunks rows written during embedding."
    },
    "chunk_count": {
      "type": "integer",
      "minimum": 1,
      "description": "Total number of chunks embedded for the job."
    }
  },
  "additionalProperties": false
}
